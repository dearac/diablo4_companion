/**
 * Overlay App — The in-game HUD window.
 * Receives build data from the main process via IPC and renders
 * it in a compact tabbed panel.
 * Placeholder for now — will be built out in Tasks 5-8.
 */
function App(): React.JSX.Element {
    return (
        <div id="overlay-root">
            <p>Overlay is ready. Waiting for build data...</p>
        </div>
    )
}

export default App
